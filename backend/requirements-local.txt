# Minimal dependencies for the ZERO-INFRA local run (SQLite + mock LLM).
# No PostgreSQL driver, no Celery/Redis/Chroma/LangGraph — those are optional
# and the app falls back gracefully without them.
#
# Version ranges (not hard pins) so pip picks wheels compatible with your
# Python version, including newer interpreters like 3.13/3.14.
#
#   pip install -r requirements-local.txt
#
# For the full production stack (PostgreSQL, LangGraph, ChromaDB, Celery),
# use requirements.txt with Python 3.11/3.12 or Docker.

fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
sqlalchemy>=2.0.30
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.1
pyjwt>=2.8
bcrypt>=4.1
pyyaml>=6.0

# testing (optional locally)
pytest>=8.0
httpx>=0.27
truststore>=0.9
