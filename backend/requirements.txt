# --- web / api ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17

# --- data / orm ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
pydantic==2.9.2
pydantic-settings==2.6.1
email-validator==2.2.0

# --- auth / security ---
pyjwt==2.10.0
bcrypt==4.2.1

# --- config / utils ---
pyyaml==6.0.2

# --- agents / orchestration (optional at runtime; graceful fallback if absent) ---
langgraph==0.2.53
langchain-core==0.3.21

# --- rag / vector (optional at runtime; in-memory fallback if absent) ---
chromadb==0.5.20

# --- cache / queue ---
redis==5.2.0
celery==5.4.0

# --- testing ---
pytest==8.3.4
pytest-cov==6.0.0
httpx==0.28.1
truststore==0.10.4
